---
description: 
globs: 
alwaysApply: true
---
# Electron应用开发指南

## 主进程和渲染进程

- 主进程 (`electron/main/`) 负责:
  - 应用生命周期管理
  - 窗口创建和管理
  - 系统集成（菜单、托盘等）
  - 安全策略设置
  - 更新管理
  - 进程间通信设置

- 预加载脚本 (`electron/preload/`) 负责:
  - 提供安全的渲染进程与主进程通信接口
  - 暴露特定的主进程功能给渲染进程

## IPC通信

- 使用上下文隔离和预加载脚本进行安全通信
- 为所有IPC消息定义明确的类型
- 主进程处理程序应考虑错误处理和超时机制

## 数据存储

- 使用`electron-store`进行配置存储
- SQLite用于结构化数据存储
- 敏感数据需加密存储

## 安全最佳实践

- 禁用`nodeIntegration`
- 启用`contextIsolation`
- 使用`CSP`(内容安全策略)
- 定义适当的`webPreferences`
- 验证深链接和外部URLs

## 应用更新

- 使用`electron-updater`处理自动更新
- 实现适当的更新通知机制
- 测试不同平台的更新流程

## 跨平台考虑

- 为所有平台路径使用`path.join()`
- 考虑操作系统特定的行为和UI
- 使用合适的缩放和DPI处理策略
